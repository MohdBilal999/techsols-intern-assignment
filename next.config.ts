import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["lxftbwnrrktcggufxfhh.supabase.co"], // 👈 your actual Supabase project domain
  },
};

export default nextConfig;
